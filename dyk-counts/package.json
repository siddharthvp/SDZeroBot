{
  "scripts": {
    "test": "node --require ts-node/register dyk-counts.ts",
    "start": "jstart -N dyk-counts -mem 4g ~/bin/node ~/SDZeroBot/dyk-counts/dyk-counts.js",
    "stop": "jstop dyk-counts || echo",
    "restart": "npm run stop && sleep 10 && npm run start"
  }
}
