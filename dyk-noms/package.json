{
  "scripts": {
    "test": "node --require ts-node/register dyknom-counts.ts",
    "start": "jstart -N dyknoms -mem 4g ~/bin/node ~/SDZeroBot/dyk-noms/dyknom-counts.js",
    "stop": "jstop dyknoms || echo",
    "restart": "npm run stop && sleep 10 && npm run start"
  }
}
