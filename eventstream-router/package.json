{
	"scripts": {
		"build": "npx tsc",
		"start": "npm run build && jstart -N stream -mem 2g ~/bin/node ~/SDZeroBot/eventstream-router/main.js",
		"stop": "jstop stream",
		"restart": "npm run stop && sleep 10 && npm run start"
	}
}
